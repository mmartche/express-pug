header.header
  nav.nav.nav--todos
    a.nav__el(href='/') All to-dos
  .header__logo
    img(src='' alt='Todos logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/default.jpg` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/sigin') Sign up